# Cloudflare Pages — nome deve coincidir com Workers & Pages → nome do projeto.
# Se o erro "Must specify a project name" continuar, edita `name` para o slug exacto no dashboard.
name = "printbag-institucional"
compatibility_date = "2026-04-08"
pages_build_output_dir = "dist"
